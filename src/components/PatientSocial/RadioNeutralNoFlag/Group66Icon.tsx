import { memo, SVGProps } from 'react';

const Group66Icon = (props: SVGProps<SVGSVGElement>) => (
  <svg preserveAspectRatio='none' viewBox='0 0 20 20' fill='none' xmlns='http://www.w3.org/2000/svg' {...props}>
    <circle cx={10} cy={10} r={10} stroke='#D30000' strokeWidth={1.5} />
  </svg>
);

const Memo = memo(Group66Icon);
export { Memo as Group66Icon };
